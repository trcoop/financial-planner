# Local-only working docs (specs, notes) — not committed
docs/

# Local-only investigative work: competitor teardowns, external formula
# validation, reference data pulls. Kept on disk on purpose — it is what the
# engine's calibration comments cite — but never committed. Nothing here is a
# source of truth; durable specs live in Linear (see CLAUDE.md).
research/

# Local design work / MCP scratch output — not committed
.design-work/
.playwright-mcp/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / build
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
